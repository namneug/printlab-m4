# CLAUDE.md — PRINTLAB: เกมสถานการณ์จำลอง 3 มิติ เครื่องพิมพ์ 3 มิติ (ฉบับ ม.4)

เอกสารนี้คือบรีฟงานสำหรับ agent ที่จะพัฒนานวัตกรรม อ่านให้ครบก่อนเขียนโค้ดบรรทัดแรก
ทุกข้อความที่ผู้เล่นเห็นเป็นภาษาไทย โค้ดและตัวระบุเป็นภาษาอังกฤษ

---

## 1. บริบทและเป้าหมาย

**ผู้เล่น** นักเรียนชั้นมัธยมศึกษาปีที่ 4 โรงเรียนสาธิตมหาวิทยาลัยราชภัฏสกลนคร รายวิชาการออกแบบและเทคโนโลยี หน่วยการเรียนรู้วิศวกรรม จำนวนประมาณ 1 ห้องเรียน (30–35 คน)

**เครื่องอ้างอิง** Creality Ender 3 V3 KE

**สิ่งที่นวัตกรรมนี้ต้องทำให้เกิด** ความรู้ความเข้าใจ 3 ด้าน (สถาปัตยกรรม การใช้งาน การซ่อมบำรุง) ทักษะกระบวนการออกแบบเชิงวิศวกรรมและการแก้ปัญหา และความคงทนของการเรียนรู้เมื่อวัดซ้ำที่ 2 และ 4 สัปดาห์

**ข้อแตกต่างสำคัญจากเวอร์ชันเดิม** เวอร์ชัน ม.1 เน้น "ประกอบเครื่องตามขั้นตอน" เวอร์ชันนี้ต้องเลิกใช้กรอบนั้น แล้วเปลี่ยนเป็น **วิเคราะห์ระบบ ตัดสินใจภายใต้เงื่อนไข และให้เหตุผลจากหลักฐาน** ห้ามพอร์ตด่านเดิมมาใช้ซ้ำ

**ตัวชี้วัดที่ผูกไว้** ว 4.1 ม.4/1 ถึง ม.4/5 (ผู้พัฒนาต้องเทียบข้อความตัวชี้วัดกับเอกสารหลักสูตรฉบับจริงอีกครั้งก่อนนำไปอ้างในเล่ม — ข้อความที่ยืนยันแล้วคือ ม.4/1 และ ม.4/5)

---

## 2. หลักการเรียนรู้ที่ต้องฝังลงในโค้ด (ไม่ใช่แค่คำโปรย)

แต่ละข้อระบุว่าเป็นข้อกำหนดจากตำรา หรือเป็นดุลยพินิจการออกแบบ เพื่อให้ผู้วิจัยอธิบายต่อกรรมการได้

| หลักการ | ที่มา | สิ่งที่ต้องเห็นในโค้ด |
|---|---|---|
| 4C/ID — whole task, learning tasks, supportive info, procedural info, part-task practice | van Merriënboer & Kirschner (2018) *Ten Steps to Complex Learning* 3rd ed. | ด่าน 5 คือ whole task ด่าน 1–4 คือ learning tasks ที่ซอยมาจากด่าน 5 · ความรู้ประกอบแสดงก่อนเริ่มด่าน · ขั้นตอนแสดงตอนที่ต้องใช้จริง ไม่ใช่กองรวมตอนต้น |
| Scaffolding ที่จางลง (fading) | van Merriënboer & Kirschner (2018) | `hintLevel` เริ่มที่ 3 ในด่าน 1–2 ลดเป็น 2 ในด่าน 3–4 และ 1 ในด่าน 5–6 · ค่านี้ต้องอยู่ในคอนฟิก แก้ได้โดยไม่ต้องแก้โค้ด |
| Stealth assessment | Shute (2011) ใน *Computer Games and Instruction* | ทุกด่านต้องมี `evidence` ที่ผูกกับ construct ที่วัด ไม่ใช่แค่คะแนนรวม · ห้ามมีหน้าจอที่บอกผู้เล่นว่า "กำลังถูกวัด" |
| Feedback: feed up / feed back / feed forward | Hattie & Timperley (2007) *The Power of Feedback* | ข้อความพี่เลี้ยงทุกชิ้นต้องระบุได้ว่าอยู่ในโหมดใดใน 3 โหมด · debrief ท้ายด่านต้องมีทั้งสามส่วน |
| กระบวนการออกแบบเชิงวิศวกรรม (EDP) | หลักสูตรแกนกลาง สาระเทคโนโลยี ว 4.1 | ด่าน 5 บังคับเดินครบ 5 ขั้นตามลำดับ ข้ามไม่ได้ และต้องวนรอบที่ 2 |
| Cognitive load — ไล่ระดับความยาก ให้ข้อมูลเมื่อต้องใช้ | Sweller / van Merriënboer | จำนวนตัวแปรที่ปรับได้เพิ่มทีละตัวตามด่าน ไม่เปิดครบตั้งแต่ด่านแรก |
| **ดุลยพินิจของผู้ออกแบบ (ไม่มีตำรารองรับโดยตรง)** | — | โครงด่านทั้ง 6 หัวข้อ · สูตรคำนวณผลการพิมพ์ในด่าน 2 · ต้นทุนเวลาของการทดสอบในด่าน 3 · เกณฑ์ผ่านของแต่ละด่าน — ทั้งหมดนี้ต้องส่งให้ผู้เชี่ยวชาญตรวจ IOC ก่อนใช้จริง |

---

## 3. Tech stack และโครงสร้างโปรเจกต์

```
printlab-m4/
  src/
    main.ts
    game/            เครื่องยนต์ฉาก การควบคุมกล้อง โหลดโมเดล
    levels/          l1-system.ts … l6-impact.ts   (หนึ่งไฟล์ต่อหนึ่งด่าน)
    modes/           explore.ts  repair.ts
    mentor/          index.ts (MentorProvider)  rule.ts  llm.ts (เฟส 2)
    telemetry/       events.ts  schema.ts  queue.ts
    ui/              คอมโพเนนต์ DOM ล้วน ไม่ใช้เฟรมเวิร์ก UI
    data/            parts.json  faults.json  materials.json  levels.json  hints.json
    styles/tokens.css
  api/               Vercel serverless functions
    events.ts
    mentor.ts        (เฟส 2 เท่านั้น)
  public/
  index.html
  package.json  vite.config.ts  tsconfig.json
```

- **Three.js + Vite + TypeScript** — เว็บ ต่อยอด WebXR ได้ในอนาคต
- **UI เป็น DOM ธรรมดา + CSS** ไม่ใช้ React/Vue เพื่อให้ไฟล์เบาและนักศึกษาอ่านโค้ดต่อได้
- **โมเดล 3 มิติ** เริ่มจาก primitive geometry ของ Three.js ทั้งหมด (กันปัญหาลิขสิทธิ์) เตรียม `GLTFLoader` ไว้สลับเป็นโมเดลจริงภายหลัง
- **ห้าม** commit ไฟล์ build ลง branch หลักโดยไม่มี source (ปัญหาของ repo เดิม)
- **ต้องมี** `.gitignore`, `README.md` (วิธี run/build/deploy), และสคริปต์ `dev` `build` `preview`

---

## 4. ระบบออกแบบ

ยึดตามดีไซน์แคนวาสที่อนุมัติแล้ว ("PRINTLAB เกมจำลองเครื่องพิมพ์ 3 มิติ") ทิศทาง "ห้องแล็บเครื่องมือวัด"

```css
:root {
  --bg:      #12161c;   --bg-deep:  #0f1319;
  --panel:   #161b22;   --panel-2:  #1a2029;   --panel-3: #212832;
  --line:    #2a323d;   --line-2:   #262e38;
  --tx:      #e9edf2;   --tx-2:     #c7d0da;   --tx-mut:  #8e9aa8;  --tx-dim: #6f7c8a;
  --amber:   #f2a13f;   /* ความร้อน วัสดุ สถานะกำลังทำ */
  --cyan:    #4fd6d2;   /* ข้อมูล การวัด พี่เลี้ยง */
  --violet:  #8f7ae0;   /* สัญญาณควบคุม */
  --green:   #6cc08d;   /* ผ่าน */
  --red:     #e4664f;   /* ไม่ผ่าน อันตราย */
}
```

- ฟอนต์: `Bai Jamjuree` (หัวเรื่อง/ตัวเลข) + `IBM Plex Sans Thai` (เนื้อความ) มี fallback เป็น `Noto Sans Thai, system-ui`
- ไอคอนเป็น inline SVG แบบเส้น (stroke) กริด 16/20/24 px **ห้ามใช้อิโมจิเป็นไอคอน**
- ปุ่มและพื้นที่กดบนมือถือไม่ต่ำกว่า 44 px
- รองรับจอ 1366×768 ขึ้นไป (ห้องปฏิบัติการคอมพิวเตอร์ 1117) และ responsive ลงมาถึงแท็บเล็ต

---

## 5. สเปกด่านและโหมด

ทุกด่านต้องมี: หน้าอธิบายแนวคิดก่อนเข้า (นิยาม + ตัวอย่างในชีวิตจริง) · เงื่อนไขสำเร็จที่ตรวจอัตโนมัติ · เงื่อนไขล้มเหลวที่กู้คืนได้ (ห้ามตกรอบ) · debrief ท้ายด่าน · จุดวัดผลฝังในเกม

### ด่าน 1 — ถอดรหัสระบบ (System Teardown) · ว 4.1 ม.4/1, ม.4/5
- ลากชิ้นส่วน 18 ชิ้นเข้า 5 ระบบย่อย: `structure` `motion` `extrusion_thermal` `control` `build_plate`
- ต่อผัง **ตัวป้อน → กระบวนการ → ผลผลิต → ข้อมูลย้อนกลับ** ของทั้งเครื่อง โดยลากเส้นเชื่อม
- สำเร็จเมื่อ: จัดกลุ่มถูกครบ และระบุวงจรป้อนกลับ thermistor → board → heater ได้ถูกต้อง
- วัด: `grouping_accuracy`, `feedback_loop_identified`, `time_on_task`, `hint_used`

### ด่าน 2 — ห้องปฏิบัติการพารามิเตอร์ (Parameter Lab) · ว 4.1 ม.4/1, ม.4/3
- ตัวแปรปรับได้ 4 ตัว: `layer_height` (0.12–0.32 มม.), `infill` (10–80 %), `speed` (30–100 มม./วิ), `nozzle_temp` (190–230 °C)
- ระบบคำนวณ 4 ผลลัพธ์แบบ deterministic: `print_time_min`, `material_g`, `strength_kg`, `surface_quality`
  - สูตรต้องอยู่ในไฟล์เดียว `src/levels/l2-params.model.ts` พร้อมคอมเมนต์อธิบายที่มาของแต่ละสัมประสิทธิ์ เพื่อให้ผู้เชี่ยวชาญตรวจได้
  - ต้องมีคำเตือนในโค้ดว่าเป็นแบบจำลองอย่างง่ายเพื่อการเรียนรู้ ไม่ใช่การจำลองเชิงฟิสิกส์
- โจทย์: ผ่านเงื่อนไข 3 ข้อพร้อมกัน (แรง ≥ 2.0 กก. · เวลา ≤ 90 นาที · วัสดุ ≤ 25 ก.)
- **บันทึกรอบทดลอง** ทุกครั้งที่กด "ทดลองพิมพ์" ระบบต้องบันทึกว่ารอบนั้นเปลี่ยนตัวแปรกี่ตัว
- วัด: `variables_changed_per_trial` (หัวใจ — วัดการควบคุมตัวแปร), `trials_count`, `converged`, `constraint_priority_order`
- พี่เลี้ยง: เมื่อเปลี่ยน ≥ 2 ตัวติดกัน 2 รอบ ให้ชี้ว่ายังสรุปไม่ได้ว่าตัวไหนมีผล **ห้ามบอกคำตอบ**

### ด่าน 3 — วินิจฉัยเชิงสาเหตุ (Root-Cause Diagnosis) · ว 4.1 ม.4/4
- เคสเริ่มต้น 6 เคสใน `faults.json`: `first_layer_not_sticking`, `layer_shift`, `nozzle_clog`, `under_extrusion`, `stringing`, `warping`
- แต่ละเคสมี: อาการ (ภาพ + ข้อความ + ข้อมูลจากเครื่อง) · สมมติฐานตั้งต้น 4 ข้อ (จริง 1 ลวง 3) · การทดสอบที่เลือกได้ 4–5 อย่าง แต่ละอย่างมี `time_cost` และ `evidence` ที่คืนมา · แผนที่ว่าหลักฐานใดตัดสมมติฐานใดออกได้
- มี **งบเวลา** จำกัด ทดสอบทุกอย่างไม่ได้
- สำเร็จเมื่อ: เลือกสาเหตุถูก **และ** อ้างหลักฐานอย่างน้อย 1 ชิ้นที่สนับสนุน
- วัด: `tests_used`, `time_spent`, `guess_without_evidence` (สรุปโดยไม่มีหลักฐาน — ตัวชี้วัดสำคัญ), `hypotheses_eliminated`, `correct_cause`

### ด่าน 4 — เลือกวัสดุและความปลอดภัย · ว 4.1 ม.4/5
- `materials.json`: PLA, PETG, ABS, TPU พร้อมสมบัติ อุณหภูมิหลอม การหดตัว ความเหนียว ความทนความร้อน ไอระเหย/การระบายอากาศ
- 3 โจทย์: ที่ยึดกล้องติดกระจกรถ (โดนแดด) · บานพับที่ต้องยืดหยุ่น · ต้นแบบโชว์ต้นทุนต่ำ
- เหตุการณ์ความปลอดภัย: ตั้งอุณหภูมิเกินพิกัดวัสดุ หรือพิมพ์ ABS ในห้องปิด → แจ้งเตือนและบันทึกเป็น safety violation
- วัด: `material_choice_correct`, `justification_from_property_table`, `safety_violations`

### ด่าน 5 — ภารกิจออกแบบเต็มวงจร · ว 4.1 ม.4/2, ม.4/3, ม.4/4
- โจทย์จากบริบทโรงเรียน เช่น ที่แขวนสายชาร์จข้างโต๊ะเรียน
- 5 ขั้นบังคับตามลำดับ: ระบุปัญหาและเงื่อนไข → รวบรวมข้อมูล → ออกแบบ (เลือก 1 จาก 3 แบบร่าง + ตั้งพารามิเตอร์) → สร้างและทดสอบ (จำลองรับแรง) → ปรับปรุงและนำเสนอเหตุผล
- บังคับวนรอบที่ 2 อย่างน้อย 1 ครั้ง
- วัด: rubric 5 องค์ประกอบ 4 ระดับ ให้คะแนนจากพฤติกรรมในเกม (ไม่ใช่ให้ผู้เล่นประเมินตัวเอง) — ต้อง export ออกมาเทียบกับ rubric ของผู้วิจัยได้

### ด่าน 6 — ผลกระทบและความยั่งยืน · ว 4.1 ม.4/1
- รวมข้อมูลจากด่าน 1–5 ของผู้เล่นเอง: เวลาเครื่องรวม พลังงานประมาณการ เศษวัสดุ (support/raft/ชิ้นเสีย)
- ประเมินผลกระทบ 4 ด้าน: มนุษย์ สังคม เศรษฐกิจ สิ่งแวดล้อม
- ตัดสินใจเชิงนโยบาย เช่น โรงเรียนควรซื้อเครื่องเพิ่มหรือจัดคิวใช้ร่วม พร้อมอ้างตัวเลขของตัวเอง
- วัด: `used_own_data_in_argument`, `impact_dimensions_covered`

### โหมดสำรวจสถาปัตยกรรม (เปิดได้ตลอด)
- แตะชิ้นส่วน → ชื่อ หน้าที่ ระบบที่สังกัด ชิ้นส่วนที่เชื่อมต่อ
- ชั้นการไหลสลับเปิดปิดได้ 3 ชั้น: สัญญาณควบคุม · การถ่ายเทความร้อน · แรงและการเคลื่อนที่
- คำถามชวนคิดฝังในบางชิ้นส่วน (เช่น ถ้าถอด thermistor ออก ระบบไหนหยุดก่อน)
- วัด: `parts_explored`, `layers_toggled`, `probe_questions_answered`

### โมดูลซ่อม (เปิดได้ตลอด)
- ใช้กลไกเดียวกับด่าน 3 แต่สุ่มเคสจากคลัง ฝึกซ้ำได้ไม่จำกัด
- **สำคัญต่องานวิจัย** ต้องออกแบบให้เพิ่มชุดเคสใหม่ได้ด้วยการเพิ่ม JSON เท่านั้น เพราะระยะติดตามผล O3 และ O4 ต้องใช้เคสชุดใหม่ที่ผู้เล่นไม่เคยเจอ

---

## 6. พี่เลี้ยง — เฟสที่ 1 คำใบ้สำรองแบบมีกฎ (ไม่ใช้ API)

**ตัดสินใจแล้วว่าเฟสแรกไม่ใช้ LLM** ยังไม่มีบัญชี Anthropic API ที่มีเครดิต จึงพัฒนาพี่เลี้ยงแบบ rule-based ที่ทำงานในเบราว์เซอร์ล้วน ไม่มีค่าใช้จ่าย ไม่มีความเสี่ยงตอบผิด และเล่นได้แม้เน็ตหลุด

**สถาปัตยกรรมที่ต้องเตรียมไว้ให้สลับได้**

```ts
interface MentorProvider {
  hint(ctx: MentorContext): Promise<MentorMessage | null>;
  explainError(ctx: MentorContext): Promise<MentorMessage>;
  debrief(ctx: MentorContext): Promise<MentorMessage>;
}
// เฟส 1: RuleMentor  (src/mentor/rule.ts)   — ใช้ตอนนี้
// เฟส 2: LlmMentor   (src/mentor/llm.ts)    — ต่อ /api/mentor ภายหลัง
```
เกมเรียกผ่าน interface นี้เท่านั้น การเปลี่ยนไปใช้ LLM ในอนาคตต้องแก้ไฟล์เดียว ไม่แตะโค้ดด่าน

**คลังคำใบ้** `src/data/hints.json` โครงสร้าง: `levelId → trigger → [ระดับ 1, ระดับ 2, ระดับ 3]`
ทุกข้อความเขียนไว้ล่วงหน้าเป็นภาษาไทย ผู้เชี่ยวชาญตรวจได้ก่อนใช้จริง ไม่เกิน 3 ประโยค

**Trigger ที่ต้องมีอย่างน้อย (เป็นเงื่อนไขจากสถานะเกม ไม่ใช่การสุ่ม)**

| ด่าน | เงื่อนไขที่ตรวจ | สิ่งที่คำใบ้ต้องทำ |
|---|---|---|
| 1 | จัดกลุ่มผิดชิ้นเดิมซ้ำ 2 ครั้ง | ถามนำว่าชิ้นนี้ทำงานร่วมกับอะไร ไม่บอกกลุ่ม |
| 1 | ยังไม่ลากเส้นวงจรป้อนกลับหลังผ่าน 3 นาที | ชี้ว่าอุณหภูมิที่วัดได้ถูกส่งกลับไปที่ใด |
| 2 | เปลี่ยน ≥ 2 ตัวแปรพร้อมกัน 2 รอบติด | ชี้ว่ายังสรุปไม่ได้ว่าตัวไหนมีผล ชวนกลับไปเปลี่ยนทีละตัว |
| 2 | ครบ 4 รอบแต่ยังไม่ผ่านเงื่อนไขเวลา | ระดับ 2 ขึ้นไปจึงบอกว่าตัวแปรใดมีผลต่อเวลามากที่สุด |
| 2 | ผ่าน 2 เงื่อนไขแต่ตัวที่ 3 แย่ลงทุกรอบ | ชี้ว่ากำลังเจอการแลกได้แลกเสีย ให้จัดลำดับความสำคัญก่อน |
| 3 | กดส่งคำวินิจฉัยโดยยังไม่เก็บหลักฐาน | ปฏิเสธการส่ง แล้วถามว่าอะไรยืนยันข้อสรุปนี้ |
| 3 | ใช้งบเวลาเกินครึ่งโดยยังไม่ตัดสมมติฐานใดออก | ถามว่าการทดสอบใดจะให้ผลต่างกันระหว่างสองสมมติฐานที่เหลือ |
| 3 | เลือกการทดสอบซ้ำกับที่ทำไปแล้ว | เตือนว่าเคยได้หลักฐานนี้แล้ว |
| 4 | เลือกวัสดุโดยไม่เปิดตารางสมบัติเลย | ชวนเปิดตารางเทียบก่อนตัดสินใจ |
| 4 | ตั้งอุณหภูมิเกินพิกัดวัสดุ หรือพิมพ์ ABS ในห้องปิด | **ข้อความคงที่ด้านความปลอดภัย** ห้ามดัดแปลง |
| 5 | ข้ามขั้น EDP | บล็อกและอธิบายว่าขั้นก่อนหน้ายังไม่ครบ |
| 5 | รอบที่ 2 ไม่เปลี่ยนอะไรจากรอบแรก | ถามว่าผลทดสอบรอบแรกบอกอะไรที่ควรแก้ |
| 6 | สรุปโดยไม่อ้างตัวเลขของตัวเอง | ชี้ไปที่ข้อมูลของผู้เล่นเองจากด่าน 1–5 |

**ระดับคำใบ้จางลง** `hintLevel` สูงสุดที่ขอได้: ด่าน 1–2 = 3, ด่าน 3–4 = 2, ด่าน 5–6 = 1
ระดับ 1 คือคำถามนำ ระดับ 2 คือชี้ทิศทาง ระดับ 3 คือชี้จุดที่ควรดู **ไม่มีระดับใดเฉลยคำตอบ**

**Debrief ท้ายด่าน** สร้างจากเทมเพลตที่เติมค่าจริงของผู้เล่น ต้องมีครบ 3 ส่วนตาม Hattie & Timperley (2007)
- feed up — เป้าหมายของด่านนี้คืออะไร
- feed back — สิ่งที่ผู้เล่นทำจริง พร้อมตัวเลขของเขาเอง (เช่น ใช้ 5 รอบทดลอง เปลี่ยนทีละตัว 2 รอบ)
- feed forward — สิ่งที่ควรทำต่างออกไปในด่านหน้า

**บันทึกเป็นข้อมูลวิจัย** ทุกคำใบ้ที่แสดงต้อง log: `trigger`, `hintLevel`, `mentorSource: "rule"`, เวลาที่แสดง, ผู้เล่นขอเองหรือระบบเสนอให้

**ผลต่อการเขียนรายงานวิจัย — ต้องอ่านข้อนี้**
ชื่อเรื่องในเค้าโครงระบุว่า "เสริมพี่เลี้ยงปัญญาประดิษฐ์ (Generative AI)" แต่เฟสนี้ยังไม่ได้ใช้ Generative AI จริง ก่อนเก็บข้อมูลต้องเลือกทางใดทางหนึ่ง
1. หาบัญชี API มาใช้ก่อนวันเก็บข้อมูล แล้วสลับเป็น `LlmMentor` ตามข้อ 6ก
2. หรือปรับชื่อเรื่องและนิยามศัพท์เฉพาะให้ตรงกับสิ่งที่ทำจริง เช่น "เสริมพี่เลี้ยงดิจิทัลแบบปรับเหมาะตามกฎ (Rule-based Adaptive Mentor)" ซึ่งยังอ้างอิง 4C/ID และ Hattie & Timperley ได้เต็มที่ และป้องกันได้ต่อกรรมการ
**ห้ามเขียนรายงานว่าใช้ Generative AI ถ้าระบบที่ใช้จริงเป็น rule-based**

---

## 6ก. เฟสที่ 2 — ต่อ LLM ภายหลัง (ยังไม่ต้องทำตอนนี้)

เมื่อมี API key แล้วค่อยเพิ่ม `src/mentor/llm.ts` + `api/mentor.ts` ตามสัญญานี้

```ts
type MentorRequest = {
  participantCode: string; levelId: string;
  gameState: Record<string, unknown>;   // ไม่เกิน 2 KB
  requestType: "hint" | "explain_error" | "debrief";
  hintLevel: 1 | 2 | 3;
};
type MentorResponse = {
  mode: "feed_up" | "feed_back" | "feed_forward";
  text: string; revealsAnswer: false;
};
```
Guardrail ที่ห้ามละเว้นเมื่อถึงเฟสนี้: API key อยู่ฝั่งเซิร์ฟเวอร์เท่านั้น · จุดวิกฤตด้านความปลอดภัยยังใช้ข้อความคงที่เสมอ · ส่ง game state ทุกครั้ง · ถ้า API ล้มเหลวให้ตกกลับมาที่ `RuleMentor` โดยอัตโนมัติและ log ว่า `mentorSource: "rule_fallback"` · rate limit ต่อผู้เข้าร่วมต่อชั่วโมง

---

## 7. ระบบเก็บข้อมูลวิจัย

**รหัสผู้เข้าร่วม** รูปแบบ `ANON-001` ถึง `ANON-050` ครูแจกให้นักเรียนคนละรหัส
**ห้ามเก็บชื่อจริง นามสกุล เลขประจำตัวนักเรียน อีเมล หรือข้อมูลระบุตัวตนใด ๆ ในระบบ** ตารางจับคู่รหัสกับชื่ออยู่นอกระบบ กับอาจารย์ที่ปรึกษาโครงการ

**Event schema**
```ts
type GameEvent = {
  eventId: string;         // uuid
  participantCode: string;
  sessionId: string;
  timepoint: "O1" | "X" | "O2" | "O3" | "O4";
  levelId: string | null;
  eventType: string;       // เช่น level_start, trial_run, hypothesis_select, test_run,
                           // evidence_received, diagnosis_submit, hint_request,
                           // safety_violation, level_complete, part_explored
  construct: "architecture" | "operation" | "maintenance" | "problem_solving" | "safety" | null;
  payload: Record<string, unknown>;
  clientTs: string;        // ISO 8601
};
```

**สิ่งที่ต้องรองรับ**
- คิวส่งแบบทนเน็ตหลุด (เก็บใน IndexedDB แล้วส่งซ้ำเมื่อกลับมาออนไลน์)
- ส่งออก CSV และ JSON แยกตาม timepoint และแยกตาม construct
- สรุปสถิติเชิงบรรยายในตัว: เวลาต่อด่าน จำนวนครั้งที่พยายาม ประเภทข้อผิดพลาด การใช้คำใบ้
- นับและแสดง **ผู้ที่ยังไม่เข้าร่วมในแต่ละจุดเวลา (attrition)** เพราะเค้าโครงระบุให้รายงานตัวเลขนี้

---

## 8. Backend

**Supabase** (ยกโครงเดียวกับโปรเจกต์ `engineering-design-snru-young` มาใช้ซ้ำ)

ตารางหลัก
- `participants` — `participant_code` (PK), `class_label`, `created_at`
- `sessions` — `session_id`, `participant_code`, `timepoint`, `started_at`, `ended_at`
- `events` — ตาม schema ข้อ 7
- `mentor_logs` — `request`, `response`, `mode`, `hint_level`, `fallback_used`, `latency_ms`
- `assessments` — คะแนนแบบทดสอบแต่ละจุดเวลา (ครูกรอกหรือ import)

RLS: เขียนได้เฉพาะผ่าน service role ในฟังก์ชันฝั่งเซิร์ฟเวอร์ ห้ามให้ anon key เขียนตรง

**Vercel serverless functions** — `api/events.ts` (เฟสที่ 1) และ `api/mentor.ts` (เฟสที่ 2)
ตัวแปรสภาพแวดล้อม: `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY` (เฟสที่ 2 จึงเพิ่ม `ANTHROPIC_API_KEY`)

---

## 9. แดชบอร์ดครูและนักวิจัย

เส้นทางแยก `/teacher` ป้องกันด้วยรหัสผ่านอย่างง่ายจากตัวแปรสภาพแวดล้อม
- ภาพรวม: จำนวนผู้เข้าร่วม เล่นครบกี่คน เวลาเฉลี่ย ผู้ที่ยังไม่เข้าร่วม O3/O4
- คะแนนเฉลี่ยตามจุดเวลา O1–O4 (แสดง "รอเก็บ" เมื่อยังไม่มีข้อมูล ห้ามแสดง 0)
- ตารางรายบุคคล: รหัสนิรนาม ความคืบหน้าราย ด่าน คะแนนแต่ละจุดเวลา จำนวนคำใบ้ที่ใช้
- ปุ่มส่งออก CSV / JSON
- ข้อความกำกับว่าแสดงเฉพาะรหัสนิรนาม

**ห้ามใส่ตัวเลขตัวอย่างค้างไว้ในระบบจริง** ตัวเลขที่เห็นในดีไซน์แคนวาสเป็นข้อมูลสมมติเพื่อดูรูปหน้าจอเท่านั้น

---

## 10. จริยธรรมและความปลอดภัย

กลุ่มตัวอย่างเป็นผู้เยาว์
- หน้าแรกต้องมีข้อความสั้น ๆ ว่าเก็บอะไรบ้าง เก็บด้วยรหัสนิรนาม และถอนตัวได้โดยไม่มีผลต่อผลการเรียน
- ไม่มีการเข้าสู่ระบบด้วยบัญชีใด ๆ ไม่มีการเก็บ IP ไม่มีการติดตามข้ามเว็บ ไม่มีโฆษณา
- ไม่มีลิงก์ออกไปภายนอกในหน้าที่นักเรียนใช้
- ข้อมูลทั้งหมดรายงานในภาพรวม

---

## 11. Milestone — สร้างทีละขั้น หยุดให้ตรวจทุกขั้น

| # | ส่ง | เกณฑ์ผ่าน |
|---|---|---|
| M0 | โครงโปรเจกต์ Vite + TS + Three.js, tokens.css, README | `npm run dev` เปิดฉากว่างที่มีกริดได้ |
| M1 | โมเดลเครื่องพิมพ์จาก primitive + กล้องหมุน/ซูม | หมุนดูรอบเครื่องได้ลื่นบนโน้ตบุ๊กทั่วไป |
| M2 | เชลล์ UI: หน้าเริ่ม กรอกรหัส แผนที่ภารกิจ กรอบหน้าจอด่าน | เดินจากหน้าเริ่มถึงหน้าด่านเปล่าได้ |
| M3 | ระบบ telemetry + คิวออฟไลน์ (ยังไม่ต่อ backend เขียนลง console/IndexedDB) | เห็น event ครบทุกการกด |
| M4 | ด่าน 1 ถอดรหัสระบบ + `parts.json` ครบ 18 ชิ้น | เล่นจบด่านและได้ evidence ครบ |
| M5 | ด่าน 2 ห้องปฏิบัติการพารามิเตอร์ + โมเดลคำนวณแยกไฟล์ | บันทึกจำนวนตัวแปรที่เปลี่ยนต่อรอบได้ถูก |
| M6 | ด่าน 3 + `faults.json` 6 เคส + โมดูลซ่อม (ใช้กลไกร่วมกัน) | เพิ่มเคสใหม่ได้ด้วยการแก้ JSON อย่างเดียว |
| M7 | โหมดสำรวจสถาปัตยกรรม 3 ชั้นการไหล | แตะได้ครบ 18 ชิ้น |
| M8 | ด่าน 4 + `materials.json` + เหตุการณ์ความปลอดภัย | บันทึก safety violation ได้ |
| M9 | ด่าน 5 EDP เต็มวงจร + ด่าน 6 ผลกระทบ | ด่าน 6 ดึงข้อมูลจริงของผู้เล่นจากด่านก่อนได้ |
| M10 | `MentorProvider` + `RuleMentor` + `hints.json` ครบทุก trigger ในข้อ 6 + debrief 3 ส่วน | ปิดเน็ตแล้วยังเล่นจบได้ครบ คำใบ้ขึ้นตรงตาม trigger และ log ครบ |
| M11 | Supabase + `api/events.ts` + export CSV/JSON | ข้อมูลลงตารางครบ ส่งออกเปิดใน Excel ได้ |
| M12 | แดชบอร์ดครู `/teacher` | เห็น attrition รายจุดเวลา |
| M13 | ทดสอบบนเครื่องห้องปฏิบัติการจริง + README วิธี deploy | เล่นจบทั้ง 6 ด่านโดยไม่ค้าง |

หยุดรายงานความคืบหน้าและรอการตรวจทุก milestone ห้ามข้ามไปทำหลาย milestone รวดเดียว

---

## 12. คำสั่งเริ่มต้น (วางเป็นข้อความแรกใน Claude Code)

> อ่าน CLAUDE.md ให้ครบก่อน แล้วทำเฉพาะ M0 เท่านั้น: ตั้งโครงโปรเจกต์ Vite + TypeScript + Three.js ตามโครงสร้างในข้อ 3 สร้าง src/styles/tokens.css จากตัวแปรสีในข้อ 4 เขียน README.md ที่บอกวิธี run build และ deploy แล้วเปิด dev server ให้เห็นฉากว่างที่มีกริดพื้น จากนั้นหยุดและรายงานว่าสร้างอะไรไปบ้าง ยังไม่ต้องทำ M1
